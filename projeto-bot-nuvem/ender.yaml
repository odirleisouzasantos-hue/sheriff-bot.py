services:
  - type: worker
    name: sheriff-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python sheriff-bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.8
